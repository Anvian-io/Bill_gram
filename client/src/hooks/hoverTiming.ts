/** Hover must be held this long before a field opens or becomes editable. */
export const HOVER_OPEN_DELAY_MS = 500;

/** Grace period before closing a portaled dropdown when the pointer leaves. */
export const HOVER_DISMISS_DELAY_MS = 150;
